# NIF Reconstruction Pipeline — Python dependencies
# Install on Kaggle: pip install -r requirements.txt
# or cell by cell (Kaggle pre-installs torch/torchvision)

# Core ML
torch>=2.1.0
torchvision>=0.16.0

# Gaussian splatting — CUDA rasteriser
gsplat>=1.0.0

# Depth estimation — DepthAnything v2. Metric-Indoor/Metric-Outdoor checkpoints
# tried first (real metres); falls back to the relative checkpoint, then MiDaS.
# Install: pip install depth-anything-v2
huggingface-hub>=0.23.0

# Scale calibration — ArUco marker detection (estimate_scale() in pipeline.py)
opencv-contrib-python>=4.8.0   # opencv-python alone does NOT include cv2.aruco

# Background removal — BiRefNet / U2-Net (product matting, person extraction)
rembg[gpu]>=2.0.57          # GPU-accelerated; use rembg (no [gpu]) on CPU-only

# Segmentation — SAM 2 (Segment Anything Model 2 by Meta)
# Install: pip install git+https://github.com/facebookresearch/sam2.git
# Falls back to single-segment if unavailable

# Storage
boto3>=1.34.0
botocore>=1.34.0

# HTTP
requests>=2.31.0

# Image/video — ffmpeg must be installed separately
Pillow>=10.0.0
imageio[ffmpeg]>=2.33.0

# Supabase
supabase>=2.4.0

# Numerics
numpy>=1.24.0
scipy>=1.11.0

# 3D print pipeline
trimesh>=4.0.0
scikit-image>=0.21.0
manifold3d>=2.4.0

# Mesh compression — KEYFRAME_MESH chunk. ENABLE_DRACO_MESH in pipeline.py
# gates this: if DracoPy isn't installed or encoding throws, _extract_mesh
# falls back to the raw struct format automatically (see the try/except
# around DracoPy.encode()) — this is not a hard requirement for the pipeline
# to run, only for the compression win.
DracoPy>=1.3.0
